/*
 * Externs file for compiling with :optimization :advanced in the Google Closure Compiler
 * Without this, the compiler will munge the variable names and they will end up being undefined
*/

window.addResizeListener = function() {};
window.removeResizeListener = function() {};
